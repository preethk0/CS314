package introUT;
import java.util.Scanner;
import java.util.Random;

public class Test {
	public static final int WOWGLOBAL = 88;
	public static void main(String[] args) {
		Scanner console = new Scanner(System.in);
		Random rand = new Random();
//		System.out.println("please enter two numbers");
//		int num1 = console.nextInt();
//		int num2 = console.nextInt();
//		System.out.printf("the prod of your numbers is %d \n", num1*num2);
		int sum = 0;
		System.out.println("Enter a character to see if it is a vowel: ");
		String y = console.nextLine();
		System.out.println(isVowel(y));
		double x = 19/5;
		int xmod = (int) x;
		//System.out.println(xmod);
		}

	public static boolean isVowel(String character) {
		return character.toLowerCase().equals("a") || character.toLowerCase().equals("e") || character.toLowerCase().equals("i") || character.toLowerCase().equals("o") || character.toLowerCase().equals("u");
	}
	
	public static int anotherfnx(int repeat) {
		int x = repeat;
		repeat += 5;
		return repeat;
	}
}
